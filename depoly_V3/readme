第一次安装 编辑好user.csv 打包好相关应用，上传对应的配置文件后
	执行 deploy init
	后续升级 执行 deploy 即可

前置需求，当前脚本为简易部署脚本，集群内所有机器的root需一致，暂未实现不同主机的root不一致的需求，暂不支持sudo用户的安装

目录结构如下 
user.csv  存放机器应用对应关系  格式如下
	ip:hostname [user:app[:shutdown_port][:start_port][AJP_port] ..]
	
	主机IP 空格 用户名：应用名（完整应用名，war需要带上.war）：访问tomcat的端口:关闭tomcat使用的端口：JAP使用的端口  一台主机的应用尽量在一行  可重复用户：应用 使用空格隔开
	端口配置可不添加，不添加为不修改上次端口，全部未修改时，为默认的8080. 一个用户下以最后一个端口配置为准

soft
	初始需要安装的软件， 例如tomcat ；如果后期redis、ZK、kafka需要手动安装，也可放置在其中，并修改对应脚本。
	可修改对应的参数配置后，全局生效

conf
	redis： reis的配置文件
	rf.saas： saas的配置文件

package
	应用程序放置的目录，包含war包、静态页等，复制到tomcat目录下


---------------------------------------V2----------------------------------	

	新增ZK KAFKA  EMB安装部署

---------------------------------------V1----------------------------------	

	支持redis tomcat *.war应用的部署

